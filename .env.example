POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-long-random-password
POSTGRES_DB=jasperserver

JRS_PUBLIC_URL=https://reports.example.com/jasperserver/

# Optional local-only bind if your Caddy runs on the host instead of the
# Coolify/Docker network. See deploy/caddy/Caddyfile.example.
# JRS_HTTP_BIND=127.0.0.1
# JRS_HTTP_PORT=9090
